<%= form_for(coupon_type) do |f| %>
  <% if coupon_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(coupon_type.errors.count, "error") %> prohibited this coupon_type from being saved:</h2>

      <ul>
      <% coupon_type.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :coupon_type_name %>
    <%= f.text_field :coupon_type_name %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
