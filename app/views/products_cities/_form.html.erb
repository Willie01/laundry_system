<%= form_for(products_city) do |f| %>
  <% if products_city.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(products_city.errors.count, "error") %> prohibited this products_city from being saved:</h2>

      <ul>
      <% products_city.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :categories_city_id %>
    <%= f.number_field :categories_city_id %>
  </div>

  <div class="field">
    <%= f.label :product_id %>
    <%= f.number_field :product_id %>
  </div>

  <div class="field">
    <%= f.label :status %>
    <%= f.number_field :status %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
