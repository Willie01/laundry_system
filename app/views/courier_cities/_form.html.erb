<%= form_for(courier_city) do |f| %>
  <% if courier_city.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(courier_city.errors.count, "error") %> prohibited this courier_city from being saved:</h2>

      <ul>
      <% courier_city.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :city_id %>
    <%= f.number_field :city_id %>
  </div>

  <div class="field">
    <%= f.label :longitude %>
    <%= f.text_field :longitude %>
  </div>

  <div class="field">
    <%= f.label :latitude %>
    <%= f.text_field :latitude %>
  </div>

  <div class="field">
    <%= f.label :status %>
    <%= f.number_field :status %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
