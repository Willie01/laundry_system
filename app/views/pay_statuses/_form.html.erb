<%= form_for(pay_status) do |f| %>
  <% if pay_status.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(pay_status.errors.count, "error") %> prohibited this pay_status from being saved:</h2>

      <ul>
      <% pay_status.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :pay_status_name %>
    <%= f.text_field :pay_status_name %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
