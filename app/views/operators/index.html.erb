<p id="notice"><%= notice %></p>

<h1>Operators</h1>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Telphone</th>
      <th>Status</th>
      <th>Permission</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @operators.each do |operator| %>
      <tr>
        <td><%= operator.name %></td>
        <td><%= operator.telphone %></td>
        <td><%= operator.status %></td>
        <td><%= operator.permission %></td>
        <td><%= link_to 'Show', operator %></td>
        <td><%= link_to 'Edit', edit_operator_path(operator) %></td>
        <td><%= link_to 'Destroy', operator, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Operator', new_operator_path %>
