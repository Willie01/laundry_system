<%= form_for(logistic) do |f| %>
  <% if logistic.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(logistic.errors.count, "error") %> prohibited this logistic from being saved:</h2>

      <ul>
      <% logistic.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
