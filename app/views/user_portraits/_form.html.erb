<%= form_for(user_portrait) do |f| %>
  <% if user_portrait.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(user_portrait.errors.count, "error") %> prohibited this user_portrait from being saved:</h2>

      <ul>
      <% user_portrait.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user_id %>
    <%= f.text_field :user_id %>
  </div>

  <div class="field">
    <%= f.label :status %>
    <%= f.number_field :status %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
